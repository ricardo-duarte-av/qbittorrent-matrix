services:
  qbittorrent-matrix:
    image: ghcr.io/ricardo-duarte-av/qbittorrent-matrix:latest
    container_name: qbittorrent-matrix
    restart: unless-stopped
    volumes:
      - ./config.yaml:/app/config.yaml:ro
